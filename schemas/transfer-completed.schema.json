{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ripdock.org/schemas/transfer-completed.schema.json",
  "title": "transfer.completed",
  "description": "Endpoint notification that a file transfer completed successfully.",
  "type": "object",
  "required": ["type", "protocol_version", "conversation_id", "payload"],
  "properties": {
    "type": { "const": "transfer.completed" },
    "protocol_version": { "const": "1" },
    "conversation_id": {
      "type": "string",
      "minLength": 1
    },
    "payload": {
      "type": "object",
      "required": ["transfer_id", "size_bytes"],
      "properties": {
        "transfer_id": {
          "type": "string",
          "minLength": 1
        },
        "size_bytes": {
          "type": "integer",
          "minimum": 1,
          "maximum": 10485760
        },
        "mime_type": {
          "enum": ["image/jpeg", "image/png", "application/pdf"]
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
